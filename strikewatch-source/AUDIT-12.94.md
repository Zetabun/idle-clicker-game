# Build 12.94 – recruitment card action alignment

Build 12.94 adjusts the portrait recruitment summary card header so the shortlist star and Details button sit side by side as requested. The shortlist button remains a compact square control, aligned alongside a full Details button.

## What changed
- recruitment mobile summary header now always uses a two-column layout with operator info on the left and actions on the right
- shortlist and Details actions are forced onto a single horizontal row
- shortlist control keeps a small square footprint
- Details keeps a taller rectangular footprint beside the shortlist control
- standalone build metadata is updated to 12.94

## Notes
- recruitment card content, scouting data, negotiation flow and report toggles are unchanged
- landscape match layout, onboarding flow, save schema 19 and diagnostics schema 1 are unchanged
