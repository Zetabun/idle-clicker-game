STRIKEWATCH CONCISE HANDOFF

Read `HANDOFF.md` and `AGENTS.md` only. Use the routing table in `HANDOFF.md`
to load the relevant section of `CONTRACTS.md`, `ARCHITECTURE.md` or one
historical audit. Do not read every Markdown or `AUDIT-*.md` file.

CURRENT RELEASE
Build 12.119 — Guided Navigation Label Fix
Build ID: 12.119.0-guided-navigation-label-fix
Save schema: 19
Diagnostics schema: 1

SOURCE AND RELEASE
`strikewatch-source/` is editable.
`js/strikewatch.dev.js`, `dist/*.html` and root `cod.html` are generated or
copied release artifacts and must not be edited independently.

CURRENT HAZARD
`.menu-subtab::after` owns the active-route rail. Inactive guided targets reuse
it for `NEXT` only after resetting inherited rail geometry. Preserve a
content-sized visible badge, route-copy clearance, the ordinary active rail,
8px desktop text and the 10.5px compact meaningful-microcopy floor.

RELEASE
Update the smallest relevant authority and current audit, build, parse, run
targeted and adjacent checks, verify responsive containment where relevant,
rebuild deterministically, copy the standalone to `cod.html`, then commit and
push source/docs/artifacts together.

Historical detail is available through `CHANGELOG.md` and individual audits.
