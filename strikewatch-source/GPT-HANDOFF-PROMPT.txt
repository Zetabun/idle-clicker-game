CURRENT RELEASE: STRIKEWATCH BUILD 12.113 - DESKTOP VERSION ALIGNMENT

Build 12.113 preserves the Build 12.112 visible-version system and corrects the desktop header badge alignment only. Keep `.manager-context-topline` as one vertically centred row, give `#managerBreadcrumb` a stable line box, and keep `.manager-build-version` statically positioned with reset margins/transforms and inline-flex content centring. Retain the mobile version inside the current-page bar revealed by the existing `?` Help control. Build metadata, static labels, generated bundle, standalone output and root `cod.html` must remain in parity. Gameplay, navigation, saves, match rendering, performance systems, save schema 19 and diagnostics schema 1 are unchanged.

STRIKEWATCH BUILD 12.112 VISIBLE VERSION HEADER CONTRACT

Repository plan: root `cod.html` is the deployed GitHub Pages file; `strikewatch-source/` is the complete editable source and documentation authority; root `other/` preserves unrelated games, assets and retired automation. Read every Markdown file under `strikewatch-source/` before editing. Change and build the game there, update the relevant authority documents and current audit, then copy the verified standalone output to root `cod.html` and commit source, documentation and deployment output together. Never treat `other/` as a Strikewatch dependency without an explicit user request.

For every numbered playable release, update the document title, CSS and JavaScript asset query strings, main-menu build stamp, desktop `#managerBuildVersion` label and mobile `#mobileCommandBuildVersion` Help-bar label so they match `BUILD_VERSION` in `js/00-core.js`. Desktop must show the version in the management header. Mobile must show it inside the current-page bar revealed by the existing `?` Help control. Keep `build.py` validation that rejects stale source labels. Preserve navigation, gameplay, visuals, persistence, save schema 19 and diagnostics schema 1, and require desktop visibility, mobile Help-off/Help-on, metadata, syntax, deterministic-build and archive-integrity checks.

STRIKEWATCH BUILD 12.111 REALISTIC OPERATOR HEADS CONTRACT

Preserve Build 12.110 operator silhouettes and Build 12.109 Citadel performance while keeping Build 12.111 head work presentation-only. Retain the shared procedural skull, fitted helmet, nose-contoured face cover and elliptical goggles without changing model height, hit detection, collision, deterministic identity, pale natural skin, distance LOD, AI, movement, weapons, combat, economy, save schema 19 or diagnostics schema 1. Require head geometry, skin, corpse, attachment, performance, collision, syntax, deterministic-build and archive-integrity audits.

STRIKEWATCH BUILD 12.110 NATURAL OPERATOR SILHOUETTES CONTRACT

Preserve Build 12.109 Citadel performance while keeping Build 12.110 operator refinements presentation-only. The revised procedural torso, pelvis, joint pads and boots, plus pelvis counter-motion, torso/head stabilisation, articulated shoulders and hand-anchored elbow bends, must not change hitboxes, collision, AI, navigation, movement, weapon grips, muzzle anchors, combat, economy, save schema 19 or diagnostics schema 1. Living and fallen operators share the new geometry, operator body draw-call count remains unchanged, and weapon attachment, corpse, Citadel performance, collision-equivalence, syntax, deterministic-build and archive-integrity audits must pass.

STRIKEWATCH BUILD 12.109 CITADEL MATCH PERFORMANCE CONTRACT

Preserve the Build 12.108 Citadel map and visual presentation. Static opaque geometry may be batched and conservatively culled, but doors, warning lights, transparency and time-dependent effects must remain dynamic. Static prop collision may use a broad phase and door colliders may be cached, but the existing narrow-phase collision, navigation and line-of-sight answers must remain exact. Do not change AI, combat, map data, materials, lighting, economy, progression, save schema 19 or diagnostics schema 1. Retain the diagnostic fallback switches and require collision-equivalence, visual-path, live-demo timing, syntax, deterministic-build and archive-integrity checks.

STRIKEWATCH BUILD 12.107 RECRUITMENT CARD SURFACE ISOLATION CONTRACT

Preserve the Build 12.106 configurable management-page background while ensuring recruitment candidate cards remain visually independent from that canvas. Every recruitment candidate card, including even rows, must own an opaque navy surface. The legacy alternating `.team-market-row:nth-child(even)` treatment must never make recruitment cards translucent or allow the selected page background colour to show through.

BUILD 12.107 INVARIANTS
- `css/game.css` owns the recruitment-only surface override after the general page-background rule.
- Odd and even candidate cards must resolve to the same base background image.
- Desktop hover/focus must retain the restrained dark hover gradient from Build 12.104.
- The page-background preference continues to affect only the management canvas behind panels.
- Recruitment logic, shortlist/comparison/report state, save schema 19 and diagnostics schema 1 remain unchanged.
- Run modular/generated/standalone syntax, light-background card parity, hover-state and ZIP-integrity checks.

STRIKEWATCH BUILD 12.106 CONFIGURABLE MANAGEMENT PAGE BACKGROUND CONTRACT

Preserve the existing management-card, panel and route visual authorities while allowing the large management canvas behind those elements to be customised from Club → Configuration. The preference is cosmetic and independent of career progress. It must update immediately, persist through `strikewatch.interfaceBackgroundColour.v1`, retain an accessible dark default and never recolour match rendering, management cards, sidebar chrome or route accents.

BUILD 12.106 INVARIANTS
- `js/50-ui-menus.js` owns colour validation, shading, local-storage persistence, Configuration markup and preset/reset interactions.
- `js/70-runtime.js` delegates Configuration click, input and change events to the background preference handlers before general career handlers.
- `css/game.css` applies the preference only to the management `.menu-content` canvas and owns the Configuration-card controls.
- The default remains Command Navy `#07131d`; invalid or unavailable stored values fall back safely.
- Career save schema remains 19 and diagnostics schema remains 1; resetting or importing a career does not need to erase this independent appearance preference.
- Preserve Build 12.105 windowed-match label cleanup and all Build 12.104 HUD/blocker-link repairs.
- Run modular/generated/standalone syntax, preset/custom/reset interaction, desktop/mobile overflow, deterministic build and ZIP-integrity checks.

STRIKEWATCH BUILD 12.105 WINDOWED MATCH LABEL CLEANUP CONTRACT

Preserve the Build 12.104 scoreboard/objective/viewport separation. The obsolete decorative `LIVE // SECURE FEED` pseudo-label must remain disabled in windowed match views so no legacy text can appear behind the HUD. Do not move the scoreboard or objective back into the live viewport. Landscape and maximised match behaviour, gameplay, save schema 19 and diagnostics schema 1 remain unchanged.

BUILD 12.105 INVARIANTS
- `css/game.css` keeps `body[data-view-mode='windowed'] .match-view::before` non-rendering with `content: none` and `display: none`.
- The scoreboard and objective remain direct match-stage rows above `.match-stage-viewport` in portrait windowed presentation.
- Do not reintroduce decorative text behind the HUD.
- Preserve Build 12.104 blocker routing and restrained recruitment hover.
- Run CSS pseudo-content, modular/generated syntax, deterministic build and ZIP-integrity checks.

STRIKEWATCH BUILD 12.104 BLOCKER LINKS AND PORTRAIT HUD REPAIR CONTRACT

Preserve the existing management-action, recruitment-card and match-HUD authorities while retaining these repairs. Blocking **Must Respond** actions must always open their actionable destination even when the opening-week progressive interface would normally lock that route. Pending sponsorship proposals specifically expose Commercial and retain exact `sponsor:<offer-id>` targeting. Desktop recruitment hover must stay dark and restrained rather than reverting to a silver/washed-out surface. In portrait windowed matches, the round scoreboard and objective are separate rows above the 16:9 live viewport; they must not overlay the canvas.

BUILD 12.104 INVARIANTS
- `js/39-workflow-integrity.js` owns blocker-action routing and exact arrival targeting.
- `js/50-ui-menus.js` may expose a progressively locked route when that route contains a live blocking decision.
- `js/00-core.js` creates one `.match-stage-viewport`; do not create duplicate wrappers or move the scoreboard/objective back into the viewport.
- `css/game.css` owns the portrait three-row match-stage layout and the restrained desktop recruitment hover.
- Landscape and maximised match layouts remain overlays over a full-stage viewport.
- Preserve sponsorship economics, recruitment calculations, match balance, save schema 19 and diagnostics schema 1.
- Run modular/generated/standalone syntax, deterministic rebuild, blocker-link click/target, portrait geometry, landscape geometry, desktop hover and ZIP-integrity checks.

STRIKEWATCH BUILD 12.96 STREAMLINED OPPONENT PREPARATION CONTRACT

Preserve the existing opponent intelligence and match-preparation gameplay. Do not create a second counter system or hidden result modifier. The default Tactics presentation must show only a quick opponent read, one recommended response and one optional match focus. Full scouting facts, alternative response templates and the complete fixture-focus list remain available through one explicit expandable control. Selecting a response continues to stage the existing formation, approach, engagement and priority fields; selected focuses continue to drive post-match evidence rather than granting invisible attributes.

BUILD 12.96 STREAMLINED-OPPONENT-PREPARATION INVARIANTS
- `js/39-matchday.js` remains the owner of response-plan generation, selection, fixture focuses, preparation snapshots and post-match evaluation.
- `js/39-opposition-intelligence.js` remains the owner of opponent identities, scouting depth, report uncertainty and staff/infrastructure effects.
- The compact briefing must not reveal information above the current scouting-depth thresholds.
- One recommendation and one optional focus are visible by default; alternatives remain available but collapsed.
- Advanced disclosure state is transient UI state and must not alter save schema 19.
- Response plans change the existing tactical controls only. No hidden win bonus, damage modifier or scripted result may be introduced.
- Preserve the existing post-match opponent-preparation review and route-linked coaching recommendations.
- Run modular/generated/standalone syntax, deterministic rebuild, compact/advanced interaction, responsive containment and runtime-cleanliness checks.

STRIKEWATCH BUILD 12.85 ONBOARDING FOCUS & SAVED MAIL CONTRACT

Read every Markdown file before editing. Preserve the First Match Journey as the single onboarding action path. While `firstMatchGuidance()` is active, the Operations Command Centre must show only the guided journey, optional context, compact club identity and Team XP; normal dashboard intelligence must return automatically when the guide completes. Preserve the full dashboard outside onboarding. Keep mail task-led: unread messages and unresolved decisions belong in Inbox, read unsaved messages leave Inbox, and saved messages remain available in Saved Mail. Decision-required emails must never disappear before their decision is resolved. The per-message `saved` flag is additive under save schema 19; the active folder is transient UI state.

BUILD 12.85 ONBOARDING-FOCUS-SAVED-MAIL INVARIANTS
- `js/36-team-management.js` owns onboarding Command Centre simplification through the existing `firstMatchGuidance()` authority; do not create a parallel tutorial-completion state.
- Hide only the normal Command Centre dashboard blocks during onboarding. Do not delete their data, routes or post-onboarding presentation.
- `js/39-club-operations.js` owns Inbox/Saved Mail filtering, read state, save state, decision-message protection and mail interaction handling.
- Inbox contains unread mail plus unresolved decision mail. Read unsaved mail is hidden; Saved Mail contains every message with `saved: true`.
- Save/unsave must be available from both the message reader and full-email modal. Marking a message unread returns it to Inbox.
- Saved mail must receive retention priority when the mail store is trimmed. Unresolved decision mail must also be protected.
- Preserve mail generation, decisions, End Day blockers, onboarding progression, gameplay, economy, infrastructure, save schema 19 and diagnostics schema 1.
- Run modular/generated/standalone syntax, deterministic rebuild, onboarding-content, Inbox disappearance, save/unsave, mobile/desktop containment, runtime-cleanliness and ZIP-integrity checks.

STRIKEWATCH BUILD 12.84 PORTRAIT MOBILE BOTTOM CLEARANCE CORRECTION CONTRACT

Read every Markdown file before editing. Preserve the fixed portrait department bar and keep `.menu-layout` as the single owner of bottom-navigation clearance. Do not add a second full navigation-height inset to `.menu-content`; portrait route content should retain only its normal content padding so the internal `#menuContent` scroller reaches the department bar without a false blank band. Preserve compact-landscape navigation, desktop layout, Command Centre ordering, recruitment, infrastructure, gameplay, save schema 19 and diagnostics schema 1.

BUILD 12.84 PORTRAIT-MOBILE-BOTTOM-CLEARANCE INVARIANTS
- `.menu-layout` retains the existing fixed-nav clearance and safe-area handling.
- Portrait `.menu-content` uses only a small normal bottom inset, not another 62–74px navigation reservation.
- The next route section must flow immediately after Team XP when scrolling the Command Centre.
- The fixed department bar must remain visible, touchable and free of content overlap.
- Compact landscape and desktop spacing remain unchanged.
- Run syntax, deterministic rebuild, portrait viewport geometry, compact-landscape regression and ZIP-integrity checks.

STRIKEWATCH BUILD 12.83 MOBILE RECRUITMENT TOUCH & FLIP RESTORATION CONTRACT

Preserve the Build 12.80 compact mobile recruitment summary, the Build 12.77 carousel navigation, the Build 12.75 dark boardroom card identity and the Build 12.71 comparison tray. Below `820px`, touch, focus, active and emulated-hover states must never replace the dark card surface with a legacy silver fill. Keep a clearly visible **Flip for Details** control on the compact summary and a **Summary** return control on the mobile scouting side. The mobile back must expose role brief, key attributes, medical, market history and Active Five impact without changing candidate data or negotiation behaviour. Keep flip state transient and preserve the current candidate across rerenders.

BUILD 12.83 MOBILE-RECRUITMENT-TOUCH-FLIP INVARIANTS
- Mobile card hover/touch/focus states retain the dark boardroom fill; only restrained border/glow accents may change.
- Shortlisted and compared cards retain distinct gold and teal accents in every interaction state.
- The compact summary remains the default mobile side and still exposes name, role, ability, potential, fee, wage, report and Negotiate.
- Flip for Details opens the dedicated mobile scout side; Summary returns to the compact side without changing scroll position.
- Desktop flip cards and compact-landscape behaviour above `820px` remain unchanged.
- Flip/report/comparison UI state remains transient and outside the career save.
- Run syntax, deterministic rebuild, touch-hover stability, mobile flip/return, report expansion, comparison persistence, responsive overflow and ZIP-integrity checks.

STRIKEWATCH BUILD 12.82 MOBILE COMMAND CENTRE PANEL HEIGHT CORRECTION CONTRACT

Read every Markdown file before editing. Preserve the compact-landscape two-column Command Centre dashboard while ensuring Objectives and Recommended Actions size independently. The shorter Manager Priority Queue must not stretch to the height of the taller objectives panel or leave a large false blank area above the persistent mobile navigation. Preserve portrait stacking, mobile navigation geometry, Command Centre action logic, Club Infrastructure, gameplay, persistence, save schema 19 and diagnostics schema 1.

BUILD 12.82 MOBILE-COMMAND-CENTRE-PANEL-HEIGHT INVARIANTS
- Keep `.command-planning-grid` content-aligned rather than stretch-aligned.
- Keep `.command-objectives-panel` and `.command-actions-panel` content-sized.
- Do not remove actions, change priority ordering or alter the compact-landscape two-column breakpoint.
- Run compact-landscape and portrait layout checks, syntax, deterministic rebuild and ZIP integrity.

STRIKEWATCH BUILD 12.81 PERSISTENT CLUB INFRASTRUCTURE CONTRACT

Read every Markdown file before editing. Preserve `js/39-infrastructure.js` as the permanent facility authority and keep the six branches connected to existing systems: Training, Scouting, Medical & Recovery, Youth Academy, Analysis and Commercial & Supporters. Every branch has four sequential levels. Projects spend Club Cash immediately, take club days to complete, use one construction queue and permanently consume division-based capacity. No refund, downgrade, cancellation or respec path may be introduced without an explicit redesign. Division capacity remains lower than the complete twenty-four-level tree so clubs must specialise. Keep infrastructure state additive under save schema 19 and preserve all existing recruitment, training, medical, opposition, commercial, supporter and economy authorities.

BUILD 12.81 PERSISTENT-CLUB-INFRASTRUCTURE INVARIANTS
- Keep `careerState.infrastructure` additive and normalised for older saves.
- Keep one active project maximum and reserve one capacity slot as soon as funding is committed.
- Keep completed levels irreversible and persistent through seasons, promotion and relegation.
- Training affects technical training progress; Scouting affects recruitment knowledge and assignments; Medical affects fatigue/injury recovery and risk; Academy creates fee-free club prospects; Analysis affects player XP and opposition reports; Commercial affects sponsor offers, match income and positive supporter growth.
- Keep academy prospects protected from rival bids and routine market rotation until their availability window expires.
- Do not create a parallel infrastructure currency. All projects use Club Cash and the existing finance ledger.
- Run source/generated/standalone syntax, deterministic rebuild, project progression, capacity, academy, effect-integration, responsive containment and ZIP-integrity checks.

STRIKEWATCH BUILD 12.80 MOBILE RECRUITMENT COMPACT SUMMARIES CONTRACT

Read every Markdown file before editing. Preserve the Build 12.75 dark-boardroom desktop recruitment cards, the Build 12.77 mobile carousel navigation and the Build 12.71 sticky comparison tray, while replacing the mobile candidate default state with a substantially shorter compact summary. On mobile and compact-tablet widths, name, role, ability, potential, fee and wage must be visible immediately, one clear primary Negotiate action must remain available without opening further panels, and the supporting scouting explanation, squad-fit detail, history/context and role brief must live inside an explicit expandable report area. Keep shortlist and comparison as compact supporting actions, preserve the sticky two/three-slot comparison tray, and keep all new report-expansion state transient UI state only. Candidate data, scouting confidence values, recommendations, transfer logic, gameplay, save schema 19 and diagnostics schema 1 remain unchanged.

BUILD 12.80 MOBILE-RECRUITMENT-COMPACT-SUMMARIES INVARIANTS
- `js/39-recruitment-commercial.js` owns the compact mobile candidate summary/report markup, the report-expansion transient state and the associated interaction handlers.
- `css/game.css` owns the compact-summary presentation and must hide the desktop flip stage below `820px` while leaving the desktop boardroom cards authoritative above that breakpoint.
- Keep mobile candidate scanning focused on name, role, ability, potential, fee and wage; do not hide those values behind the expanded report.
- Keep exactly one primary Negotiate action visible in the collapsed summary. Secondary actions may support shortlist, comparison, report expansion and full-profile access only.
- Preserve and visibly retain the sticky comparison tray so two or three selected candidates stay available while browsing the market.
- Do not write report-expanded state, comparison state or shortlist state to the career save beyond the existing shortlist authority.
- Run source/generated/standalone syntax, deterministic rebuild, compact-summary interaction, responsive containment, runtime-cleanliness and ZIP-integrity checks.

STRIKEWATCH BUILD 12.79 ACCESSIBILITY & VISUAL HIERARCHY CONTRACT

Read every Markdown file before editing. Preserve `css/game.css` as the presentation authority, `js/50-ui-menus.js` as the First Match Journey presentation authority, `js/39-club-operations.js` as the Inbox presentation/behaviour authority and `build.py` as the generator of `js/strikewatch.dev.js` and the standalone release. Preserve the Build 12.78 desktop Inbox layout, Build 12.77 mobile recruitment carousel navigation and all gameplay/persistence authorities.

BUILD 12.79 ACCESSIBILITY-HIERARCHY INVARIANTS
- Disabled controls must remain clearly unavailable without low-opacity or dark-on-dark text. Neutralise primary gold when a control is disabled.
- Enabled keyboard-focus targets must show the high-contrast cyan outline and dark separation halo without changing layout.
- Keep informational panels visually quieter than actionable controls. Interactive surfaces retain clear borders, hover feedback and pointer affordance.
- Routine secondary guidance and Inbox metadata use sentence case and reduced letter spacing; action labels and compact status headings may remain uppercase.
- Reserve gold for primary actions, scouting ratings, recommendations and genuinely important states. Routine labels, panel caps, badges and scrollbars use cyan/teal information accents.
- The First Match Journey action must remain a bright, readable cyan control on desktop and mobile.
- Preserve gameplay, recruitment calculations, mail decisions, economy, save schema 19 and diagnostics schema 1.
- Run syntax, deterministic rebuild, disabled contrast, keyboard focus, guided-action visibility, sentence-case copy, gold-restraint, supported viewport overflow, runtime and ZIP-integrity checks.

STRIKEWATCH BUILD 12.72 MOBILE RECRUITMENT READABILITY & LAYOUT INTEGRITY CONTRACT

Read every Markdown file before editing. Preserve `css/game.css` as the presentation authority, `js/39-recruitment-commercial.js` as the recruitment-content and interaction authority, and `build.py` as the generator of `js/strikewatch.dev.js` and the standalone release. Preserve the Build 12.71 flip and comparison systems. Below 1024 pixels, keep candidate text and rating stars readable, keep one full-width candidate card per row, reserve explicit space for the squad-fit guidance above the actions, and keep the reverse face free of nested scrolling. The expanded First Match Journey and comparison tray must remain in normal flow; only the collapsed journey summary may be sticky. Preserve candidate values, recommendation factors, transfer terms, negotiation/signing logic, save schema 19 and diagnostics schema 1.

BUILD 12.72 MOBILE-RECRUITMENT-LAYOUT INVARIANTS
- No overlap is permitted between `.recruitment-card-fit` and `.recruitment-card-actions` at supported mobile widths.
- `.recruitment-card-back-body` must fit its content without an internal vertical scrollbar at supported mobile widths.
- Mobile metric labels, financial values, squad-fit copy and ability/potential stars must remain legible and financial ranges must not be lost to ellipsis.
- Expanded onboarding and the comparison tray must not cover each other or candidate content. The collapsed journey summary may remain sticky.
- Preserve explicit keyboard-operable flip controls, `aria-hidden`/`inert` face ownership, comparison selection limits and transient interface state.
- Run mobile portrait, compact landscape, tablet, desktop, interaction, overflow, syntax, deterministic rebuild and ZIP-integrity checks.

STRIKEWATCH BUILD 12.71 COMPACT RECRUITMENT & COMPARISON TRAY CONTRACT

Read every Markdown file before editing. Preserve `js/39-recruitment-commercial.js` as the Recruitment commercial-flow authority, `css/game.css` as the complete presentation authority and `build.py` as the generator of `js/strikewatch.dev.js` and the standalone release. Preserve the Build 12.70 navy/pitch-green/gold interface. Recruitment market cards use an explicit two-face interaction: keep decision-critical facts and direct actions on the front, deeper scouting context on the back, and the full operator profile as the authoritative report. Keep card flip, comparison selection and expanded-workspace state transient. Preserve recruitment data, market ordering, transfer terms, signing logic, career persistence, save schema 19 and diagnostics schema 1.

BUILD 12.71 COMPACT-RECRUITMENT-COMPARISON-TRAY INVARIANTS
- Keep the front face readable without flipping: identity, role, scouting confidence, ability, potential, fee, wage, squad-fit interpretation and shortlist/compare/report/negotiate actions must remain available.
- Keep flip controls explicit and keyboard-operable; update `aria-hidden` and `inert` so only the visible face is interactive, restore focus after flipping and honour reduced-motion preferences.
- Keep the sticky comparison tray bounded to three candidates, removable and clearable. Selecting a second candidate may reveal the detailed workspace but must not trigger a negotiation or signing.
- Preserve the existing squad-fit recommendation factors: immediate impact, future ceiling, current squad need and budget fit. Do not reduce the recommendation to overall ability alone.
- Do not write flip IDs, comparison IDs or expanded-state flags to `careerState`, local storage or exported diagnostics.
- Run compact-card, flip interaction, comparison interaction, supported viewport/overflow, retained recruitment/state/typography/first-match diagnostics, syntax, deterministic rebuild and ZIP-integrity checks.

STRIKEWATCH BUILD 12.70 FM INTERFACE & RECRUITMENT CLARITY CONTRACT

Read every Markdown file before editing. Preserve `index.html` as the modular document/markup authority, `css/game.css` as the complete styling authority and `build.py` as the generator of `js/strikewatch.dev.js` and the standalone release. Do not paste the generated standalone CSS or JavaScript back into `index.html`. Preserve the navy/pitch-green/gold boardroom theme, flatter table rows and the mobile Recruitment card hierarchy introduced from the user's edited HTML. Preserve Build 12.69 guided navigation, all gameplay and persistence, save schema 19 and diagnostics schema 1.

BUILD 12.70 FM-INTERFACE-RECRUITMENT-CLARITY INVARIANTS
- `index.html` contains versioned references to `css/game.css` and `js/strikewatch.dev.js`; it must not contain the full generated CSS or JavaScript bundle.
- `css/game.css` contains the complete visual theme and the mobile `.team-market-row` recruitment layout refinements.
- The theme may alter presentation, borders, fills and colour emphasis but must not create gameplay state or hidden modifiers.
- Mobile Recruitment candidates must retain clear full-card identity, readable rating/confidence information and practical action targets without horizontal overflow.
- Preserve Build 12.69 negotiation arrival and destination-specific Manager Options routing.
- Run source/generated/standalone syntax, deterministic rebuild, responsive containment, retained diagnostics and ZIP-integrity checks.

STRIKEWATCH BUILD 12.69 GUIDED NAVIGATION & ADVICE ROUTING CONTRACT

Read every Markdown file before editing. Preserve the Build 12.69 guided-arrival behaviour: beginning an incoming negotiation must route to Transfers and place the active negotiation card clearly below the sticky management chrome, including while the First Match Journey is visible. Shared guided scrolling must continue to resolve both tutorial guide targets and general management target IDs. Opposition Manager Options must use destination-specific labels and route to actionable content: Match Plan / opponent preparation, Active Five and squad loadouts. Selecting tactics advice while already on Tactics must scroll beyond the report rather than rerendering at the same advice control. Preserve negotiation values, recruitment data, tactical effects, match simulation, economy, save schema 19 and diagnostics schema 1.

BUILD 12.69 GUIDED-NAVIGATION-ADVICE-ROUTING INVARIANTS
- Keep `startIncomingTransferNegotiation()` as the negotiation-state authority, then route to Transfers and request the existing `transfer:<deal id>` target; do not duplicate or auto-submit negotiation terms.
- Keep shared guided scrolling bounded to `.menu-content`, below sticky management chrome, with route/request guards and support for both `[data-guide-target]` and `[data-management-target-id]`.
- Keep Manager Options advisory only. Destination links must not apply tactics, choose operators, equip items or create hidden bonuses.
- Keep tactics advice targeted to `tactics:opponent-preparation`, operator advice to `operators:active-five` and equipment advice to `loadout:squad-overview` unless those authoritative destination IDs are deliberately migrated together.
- Preserve Build 12.68 mobile onboarding and HUD clarity, Build 12.67 living press and honours, Build 12.66 mobile interface clarity, gameplay, persistence, save schema 19 and diagnostics schema 1.
- Run negotiation-arrival, same-page tactics-arrival, cross-page destination, supported viewport/overflow, retained diagnostics, syntax, deterministic rebuild and ZIP-integrity checks.

STRIKEWATCH BUILD 12.68 MOBILE ONBOARDING & HUD CLARITY CONTRACT

Read every Markdown file before editing. Preserve the Build 12.68 mobile corrections: new-club emblem choices must replace the visible preview SVG and label immediately; the portrait windowed-match objective must remain a slim single-line strip flush beneath the scoreboard; the mobile Inbox unread badge must remain visually centred and hidden when its zero-state authority hides it; Recruitment's operator-role guide must start closed but remain expandable; and the scouting percentage must be presented as Scouting Confidence with a clear explanation of the estimates it qualifies. Keep the internal scouting state and progression authority unchanged. Preserve desktop layout, match simulation, recruitment ordering, economy, save schema 19 and diagnostics schema 1.

BUILD 12.68 MOBILE-ONBOARDING-HUD-CLARITY INVARIANTS
- Replace only the large new-club emblem preview after `[data-team-logo]` selection; retain the selected option, identity draft, colour update and saved logo identifier authorities.
- Keep the compact objective rule portrait/windowed/mobile only, matched to the scoreboard's left/right edges, with the secondary plan line omitted to protect viewport space.
- Keep the mail badge centred with flex alignment without overriding its `[hidden]` zero state or moving the surrounding topbar cell.
- Keep the Recruitment role guide closed by default and natively expandable; do not remove its role definitions or tutorial relevance.
- Keep internal `knowledge` values, scout assignments and progression unchanged while labelling the UI **Scouting Confidence** and explaining ability, potential, fee, wage and medical estimate reliability.
- Preserve Build 12.67 living press and honours, Build 12.66 mobile interface clarity, gameplay, desktop presentation, save schema 19 and diagnostics schema 1.
- Run emblem interaction, role-guide, confidence-copy, badge/objective geometry, supported viewport, retained state/typography, syntax, deterministic rebuild and ZIP-integrity checks.

STRIKEWATCH BUILD 12.67 LIVING PRESS & PLAYER HONOURS CONTRACT

Read every Markdown file before editing. Preserve `js/56-world-press-awards.js` as the additive living-world authority. Every completed league or exhibition fixture must have one deterministic impact-based Man of the Match, including AI-versus-AI league fixtures. The non-settling guided orientation must remain reward-neutral. Pay the player club exactly once only when one of its operators wins: `1,500 CR` for a league match and `750 CR` for an exhibition. Keep the award in the after-action finance report and `AWARD` ledger. Preserve compact matchday roundups, deduplicated upcoming-opponent intelligence, rival-player histories, permanent operator accolade timelines and Team > Honours club records. Historical played fixtures may be backfilled once without replaying outcomes or mutating historical AI totals. Keep all new persistence additive under the existing schema-19 `careerState.worldPress`; fixture outcomes, combat AI, weapons, armour, economy outside award payments and diagnostics schema 1 must remain unchanged.

BUILD 12.67 LIVING-PRESS-AND-HONOURS INVARIANTS
- Give every completed fixture exactly one Man of the Match report and preserve deterministic selection for identical match data.
- Evaluate overall impact rather than kills alone; support, survival, role execution, trades, opening impact and rating must remain eligible factors.
- Apply the player-club reward once, add one `AWARD` ledger transaction and update the after-action income/net figures once.
- Keep AI fixture reports, player histories, roundup keys, intel keys and accolade entries deduplicated across saves, reloads and repeated render calls.
- Keep routine league results inside one Matchday Roundup email and cap mail through the established Inbox authority.
- Keep future-opponent reports grounded in confirmed results and clearly separate scout analysis from public information; deeper detail depends on employed scouting quality.
- Keep player accolade histories permanent and include former club operators in the club record book after transfer or release.
- Preserve schema 19 and tolerate older saves through additive defaults and one-time historical backfill.
- Run the living-world integration test, route/profile markup checks, supported viewport containment, source/generated/standalone syntax, deterministic rebuild and ZIP integrity.

STRIKEWATCH BUILD 12.66 MOBILE INTERFACE CLARITY CONTRACT

Read every Markdown file before editing. Preserve the mobile-only First Match Journey collapse control below `1024px`: the expanded panel must expose a clear chevron and the established action, while the collapsed presentation must become a 48-pixel one-line summary bar that can be expanded again. Keep this state transient and outside the schema-19 career save. Preserve stronger complete-card boundaries between Recruitment candidates without changing candidate data, comparison, scouting or negotiation behaviour. Keep mobile Inbox rows content-sized so metadata, subject and preview remain inside their own message row at supported phone and compact-landscape viewports. Desktop presentation, gameplay, save schema 19 and diagnostics schema 1 remain unchanged.

BUILD 12.66 MOBILE-INTERFACE-CLARITY INVARIANTS
- Keep the First Match Journey collapse control a real button with `aria-expanded`, an explicit accessible label and at least a 44-pixel mobile target.
- Keep the expanded journey action available and make the collapsed bar a single readable line with overflow-safe summary text.
- Do not persist the collapsed state or add it to the career save.
- Keep candidate separation presentation-only; preserve all recruitment ordering, selection, comparison and negotiation authorities.
- Keep each mobile mail row content-sized and prevent text, dates, badges or stars from overlapping neighbouring messages.
- Keep the journey toggle hidden at desktop widths and preserve the established desktop panel.
- Run focused collapse/expand, candidate-boundary and mail-row geometry tests at supported portrait and compact-landscape sizes, plus desktop isolation, syntax, deterministic rebuild and ZIP integrity.

STRIKEWATCH BUILD 12.64 MOBILE PAGE HELP TOGGLE CONTRACT

Read every Markdown file before editing. Preserve the mobile-only `managerHelpToggleBtn` below `1024px`: it replaces the mobile Match shortcut, starts white/off and toggles the contextual `mobileCommandRouteBar` beneath the topbar. When enabled, the question mark turns gold and the complete current-page bar appears with its existing **Pages** entry to the Club Navigator; when disabled, remove the bar from layout without leaving a gap. Persist only this UI preference under `strikewatch.mobilePageHelpVisible`; never add it to the schema-19 career save. Keep the persistent active department as an independent Club Navigator entry so hiding Help cannot remove route discovery. At desktop widths hide the Help control and retain the established Match shortcut. Preserve Build 12.63 Armoury optimisation, Build 12.62 navigation, Build 12.61 readability, gameplay, save schema 19 and diagnostics schema 1.

BUILD 12.64 MOBILE-PAGE-HELP-TOGGLE INVARIANTS
- Keep `managerHelpToggleBtn` a real button with `aria-pressed`, `aria-expanded`, `aria-controls` and a minimum 44-pixel mobile target.
- Keep off-state question-mark presentation white and on-state presentation gold/accented.
- Keep `mobileCommandRouteBar` hidden by default unless the stored preference is exactly enabled.
- Keep the full bar content and **Pages** action unchanged when visible; do not create a second help panel or route map.
- Store only the presentation preference in local storage and tolerate storage failure without breaking startup.
- Keep the desktop Help control hidden and desktop Match shortcut visible.
- Keep onboarding shortcut copy responsive: **HELP** on mobile, **MATCH** on desktop.
- Run focused toggle, persistence, ARIA, navigator-access and viewport tests; all-route Help-off/Help-on matrices; retained Armoury/onboarding/readability/economy/state diagnostics; syntax; deterministic rebuild; and ZIP integrity.

STRIKEWATCH BUILD 12.63 ARMOURY 3D PREVIEW OPTIMISATION CONTRACT

Read every Markdown file before editing. Preserve the Team Armoury's product-only armour detail inspector: it must mount the complete authored chest piece without `careerArmourOperatorBodyParts()` or the Supply Depot product form. Keep drag/swipe, rotate, zoom, reset, auto rotation, perspective and front/side/rear inspection. Inventory cards must use `careerArmourThumbnailParts()` from the authoritative armour definitions rather than flat images, and static thumbnails must not reserve permanent transform compositor layers or restore the removed parent drop shadow. Keep offscreen row content visibility and pause automatic inspector rotation when the viewer is outside the viewport. Preserve the Supply Depot's complete headless product forms and orbit, live WebGL armour, gameplay balance, ownership, assignments, save schema 19 and diagnostics schema 1.

BUILD 12.63 ARMOURY-3D-PREVIEW-OPTIMISATION INVARIANTS
- Keep `careerArmour3dParts()` as the complete armour geometry authority and `careerArmourThumbnailParts()` as its compact list-level derivative.
- Keep the selected inspector product-only and retain every authored part of the selected vest; remove only the unrelated operator/form geometry.
- Keep inventory thumbnails full-depth CSS 3D, with recognisable shell, plate, strap, shoulder/neck, pouch and class-specific features.
- Keep static Armoury models at `will-change: auto`; promote only active drag or auto-rotation.
- Keep auto rotation paused when the inspector is hidden or outside the viewport.
- Preserve Supply Depot store forms/orbit and live WebGL operator armour unchanged.
- Run `armour3dPresentationForTest()`, armour loadout mapping, real rotate/zoom/drag/auto checks, all-route viewport geometry, retained onboarding/readability/state diagnostics, syntax, deterministic rebuild and ZIP integrity.

STRIKEWATCH BUILD 12.62 MOBILE NAVIGATION CONSOLIDATION CONTRACT

Read every Markdown file before editing. Preserve one mobile navigation hierarchy below `1024px`: the five departments are the persistent primary control, the current-page bar is the only subsection entry point, and the full-screen Club Navigator is the only mobile all-pages/search directory. Portrait departments belong in the safe-area-aware bottom bar; short landscape departments belong in the compact left rail. Keep the legacy horizontal subsection carousel, Command Index and section-hub page directories hidden on mobile but unchanged on desktop. Tapping a different unlocked department opens its overview; tapping the active department or PAGES opens the navigator; tapping a locked department opens its navigator section with the exact unlock reason. Preserve global search, exact progressive-lock reasons, current-page/notification state, first-match task, X/Escape close, focus containment/return, body scroll lock and pause-session Return/Exit actions. The navigator must default closed and must not persist UI-only state. Preserve Build 12.61 mobile readability, Build 12.60 desktop presentation, Build 12.59 onboarding, gameplay, save schema 19 and diagnostics schema 1.

BUILD 12.62 MOBILE-NAVIGATION-CONSOLIDATION INVARIANTS
- Keep exactly five persistent mobile departments and all authoritative routes from `menuSections`; do not create a second route map.
- Keep mobile route discovery in the Club Navigator and desktop route discovery in the established subsection grid/Command Index.
- Keep portrait bottom navigation clear of scroll content and compact-landscape navigation in the slim left rail.
- Keep all navigator actions at least 44px and all meaningful navigator text at or above the Build 12.61 mobile floor.
- Keep the dialog closed by default, safe-area aware, scroll-contained, keyboard closable and focus trapped while open.
- Do not save browsed department, search query or open state.
- Run navigator interaction tests, all-route viewport matrices, retained onboarding/readability/state diagnostics, syntax, deterministic rebuild and ZIP integrity.

STRIKEWATCH BUILD 12.61 MOBILE READABILITY & UX PASS CONTRACT

Read every Markdown file before editing. Preserve the final mobile-only readability authority below `1024px` in `css/game.css`: meaningful mobile text must not fall back below the 9px microcopy floor, explanatory and decision copy should use larger semantic tokens, recurring standalone actions must retain 44px touch targets and disabled content must remain legible. Preserve the real narrow-phone labels `OPS`, `GEAR` and `SUPPLY` without duplicated pseudo-content. At `430px` and below, preserve the current/event-date calendar list; compact landscape and desktop retain the month grid. Preserve the mobile live-match density rules: guided portrait feed messages must not cover the objective, mobile match controls remain 44px, and compact landscape uses one readable telemetry card without the duplicate spectator card or four-field decision grid. Keep Build 12.60 desktop typography and layout unchanged at `1024px` and wider. Preserve gameplay, renderer, AI, economy, persistence, save schema 19 and diagnostics schema 1.

BUILD 12.61 MOBILE-READABILITY-UX INVARIANTS
- Keep mobile hierarchy and decision copy readable at 320, 375, 390 and 430 CSS pixels and at 844 × 390 landscape.
- Keep standalone mobile controls at least 44px and preserve safe-area, scrolling and route-navigation ownership.
- Keep full desktop labels and real short portrait labels; never hide source words with a zero font size.
- Keep the narrow calendar list limited to current and event-bearing dates, with complete date text and 44px event actions.
- Keep portrait live objectives, score, spectator dock and utility actions readable; guided orientation suppresses duplicate feed rows.
- Keep compact-landscape telemetry to one readable card with the plain-language decision reason; restore full decision detail only on taller layouts.
- Preserve Build 12.60 desktop readability, Build 12.59 onboarding, Build 12.58 Command Centre, save schema 19 and diagnostics schema 1.
- Run mobile first-session, 24-route viewport matrices, typography/onboarding/state diagnostics, source/generated/standalone syntax, deterministic rebuild and ZIP-integrity checks.

STRIKEWATCH BUILD 12.60 DESKTOP READABILITY PASS CONTRACT

Read every Markdown file before editing. Keep all Build 12.60 readability work inside the final `@media (min-width: 1024px)` desktop layer in `css/game.css`. Preserve the established interface below 1024 pixels exactly. Retain the scalable desktop typography tokens for micro labels, labels, supporting copy and body text; ordinary Command Centre explanations and management helper copy must remain readable at the 1024-pixel baseline and scale modestly on wide displays. Disabled and locked content must remain visibly inactive without becoming illegible. Preserve the Build 12.59 onboarding flow, Build 12.58 desktop layout authority, gameplay, renderer, economy, persistence, save schema 19 and diagnostics schema 1.

BUILD 12.60 DESKTOP-READABILITY-PASS INVARIANTS
- Preserve `--desktop-type-micro`, `--desktop-type-label`, `--desktop-type-small` and `--desktop-type-body` in the desktop-only CSS layer.
- Keep Active Club Objectives, Manager Priority Queue, momentum and Command Index titles, descriptions and metadata readable at 1024 pixels and wider.
- Keep topbar, sidebar and long-term management helper text subordinate but not phone-sized or dependent on very low opacity.
- Do not change mobile portrait, mobile landscape, touch targets, safe areas, route overflow, live-match layout or gameplay.
- Preserve the Build 12.59 six-milestone/nine-state onboarding flow and focused Recruitment presentation.
- Run `typographyConsistencyForTest()`, all-route desktop/mobile geometry, source/generated/standalone syntax, deterministic rebuild and ZIP-integrity checks.

STRIKEWATCH BUILD 12.59 FIRST MATCH ONBOARDING REFINEMENT CONTRACT

Read every Markdown file before editing. Preserve `firstMatchGuidance()` as the only opening-journey authority and keep its nine internal state checks compatible with existing schema-19 saves. Present those states as six player-facing milestones only; do not persist a second checklist. Before the first completed match, while the squad contains fewer than five operators and the guide is in its recruitment sequence, Recruitment may use the transient focused presentation with six deterministic real-market candidates, Active Five progress and existing comparison/transfer actions. Keep the complete 18-player scouting department one explicit click away. Do not save the advanced-view toggle, create a second market, auto-sign, alter candidate ratings or bypass ordinary transfer affordability, wages, fees, negotiations and squad limits. Once the orientation demo is complete, hide its obsolete skip action. Preserve the Build 12.58 desktop layer, the established mobile interface below 1024 pixels, progression, gameplay, economy, save schema 19 and diagnostics schema 1.

BUILD 12.59 FIRST-MATCH-ONBOARDING-REFINEMENT INVARIANTS
- Keep the same nine internal first-match state IDs and route transitions; six visible milestones are derived presentation only.
- Focused Recruitment exists only for the first pre-match squad-building phase with fewer than five contracted operators and uses no persisted beginner-mode field.
- `recruitmentBeginnerCandidates()` returns six deterministic, affordable and role-diverse candidates from the authoritative current market.
- Preserve reports, shortlisting, comparison, negotiation, transfer finances, wages, squad limits and the Build 12.47 guided negotiation route exception.
- Keep all 18 listings, scouting pools, search commissioning and market movement accessible through one explicit advanced-tools control and as the normal long-term Recruitment experience.
- Derive `ACTIVE FIVE N / 5` signing progress from the contracted squad; do not save a duplicate counter.
- Keep the creation action before optional emblem customisation and keep the first recommended candidate in the initial supported desktop viewport.
- Hide the demo skip button only after completion; preserve the ordinary skip path while orientation is incomplete.
- Run onboarding, recruitment, progression, typography, state-integrity, route-matrix, mobile-containment, syntax, deterministic-build and archive-integrity checks.
- Preserve Build 12.58 desktop, Build 12.57 skin, Build 12.56 weapons, Build 12.55 operators, Build 12.54 armour, Build 12.53 commands, Build 12.52 arenas, save schema 19 and diagnostics schema 1.

STRIKEWATCH BUILD 12.58 DESKTOP COMMAND CENTRE CONTRACT

Read every Markdown file before editing. Preserve the established phone interface exactly as the visual authority below 1024 CSS pixels. Desktop Command HQ changes belong in the final `@media (min-width: 1024px)` layer of `css/game.css`; shared markup may expose semantic hooks only when those hooks remain hidden or inert on mobile. Preserve the 80-pixel command bar, labelled desktop shortcuts, 252/292/304-pixel section rail, dynamic department identity, non-scrolling width-aware route grid, centred bounded workspaces, section-specific desktop grids and two-pane Inbox. Keep `js/50-ui-menus.js` as the existing route/navigation authority. Do not create a second state model or change gameplay, renderer geometry, AI, combat, economy, save schema 19 or diagnostics schema 1.

BUILD 12.58 DESKTOP-COMMAND-CENTRE INVARIANTS
- Keep every desktop presentation override guarded by `@media (min-width: 1024px)` unless a future task explicitly changes mobile.
- Preserve mobile portrait and 844 × 390 landscape geometry, touch sizing, safe areas, route arrows, route-scroll ownership and the Build 12.53 commentary/command dock.
- Keep the desktop topbar at 80 pixels and the section rail at 252 pixels for compact PC widths, 292 pixels from 1280 pixels and 304 pixels from 1440 pixels.
- Keep desktop route navigation free of horizontal scrolling and arrow controls. It may wrap, but its measured height must remain fully reserved above `.menu-content`.
- Keep desktop workspaces centred, bounded and readable at 1024, 1280, 1366, 1440 and 1920 CSS pixels with visible keyboard focus and zero document horizontal overflow.
- Preserve `menuCommandKicker`, `menuCommandTitle` and `menuCommandSubtitle` as presentation hooks updated by `updateMenuUI()` rather than state authorities.
- Run mobile visual/geometry parity, desktop route matrix, source/generated/standalone syntax, deterministic rebuild, retained debug regressions and archive-integrity checks.
- Preserve Build 12.57 operator skin, Build 12.56 connected weapons, Build 12.55 operator geometry, Build 12.54 full-depth armour, Build 12.53 commands, Build 12.52 arena geometry, save schema 19 and diagnostics schema 1.

STRIKEWATCH BUILD 12.57 LIGHT OPERATOR SKIN CONTRACT

Read every Markdown file before editing. Preserve `OPERATOR_SKIN_PRESENTATION` in `js/62-character-renderer.js` as the deterministic live WebGL complexion authority, with exactly six pale natural tones rather than a flat pure-white material. Preserve `OPERATOR_SKIN_MATERIAL` in `js/60-renderer-core.js` as the dedicated matte skin-lighting authority: surface 8, roughness 0.64 and ambient lift 0.42. Keep the face cover at or below 0.70 of head width and 0.36 of head height so cheeks, upper jaw and neck remain visible. Living and fallen operators must use the same material through `drawOperatorHeadAssembly()` and their neck segments. Keep operators gloved. Align the Armoury mannequin's `.operator-skin` face/neck gradient with the live direction, but keep Supply Depot product forms headless. Do not change identity data, attributes, collision, hitboxes, AI, combat, weapons, armour, economy, save schema 19 or diagnostics schema 1.

BUILD 12.57 LIGHT-OPERATOR-SKIN INVARIANTS
- Preserve `OPERATOR_SKIN_PRESENTATION`, `OPERATOR_SKIN_MATERIAL`, `operatorVisualProfile()` and `operatorSkinPresentationAudit()`.
- Keep six deterministic pale natural variants and avoid a self-illuminated or unshaded pure-white face.
- Keep surface 8, roughness 0.64 and ambient lift 0.42 aligned across live and corpse head/neck paths.
- Keep the face-cover exposure ratios within the documented 0.70 width / 0.36 height limits.
- Keep gloves, face covering, helmet, armour and clothing as separate non-skin materials.
- Keep the Armoury mannequin's mounted computed skin material light and natural; do not add a head to Supply Depot product forms.
- Run `operatorSkinPresentationForTest()`, operator head/live/held/corpse/first-elimination checks, `armour3dPresentationForTest()`, connected-weapon checks and all arena/navigation regressions.
- Preserve Build 12.56 weapon geometry, Build 12.55 operator meshes, save schema 19 and diagnostics schema 1.
- Validate syntax, live WebGL, page-console cleanliness, deterministic rebuild parity and archive integrity before release.
STRIKEWATCH BUILD 12.56 CONNECTED WEAPON GEOMETRY CONTRACT

Read every Markdown file before editing. Preserve `careerWeaponVisualParts()` as the one authored weapon-part authority. Live world-held, corpse-held and first-person WebGL renderers must obtain their scales from `CAREER_WEAPON_RENDER_SCALES` through `careerWeaponRenderScaleProfile()`; vertical part positions and vertical part sizes must use the same scale so authored neighbours cannot be stretched apart. Keep `magazine` and `mag-base` on the shared `careerWeaponPartMovesWithMagazine()` reload transform. Preserve the seated sidearm trigger guards and connected AR-4 rear aperture bridge. Do not change weapon damage, accuracy, fire rate, range, penetration, magazine capacity, reload timing, prices, ownership, AI, animation anchors, save schema 19 or diagnostics schema 1.

BUILD 12.56 CONNECTED-WEAPON INVARIANTS
- Preserve `CAREER_WEAPON_RENDER_SCALES`, `careerWeaponRenderScaleProfile()`, `careerWeaponPartMovesWithMagazine()`, `careerWeaponRenderedPartBounds()`, `careerWeaponRenderedPartGap()` and `careerWeaponGeometryIntegrityAudit()`.
- Keep `verticalPositionScale === verticalSizeScale` for long guns and sidearms in world and viewmodel contexts.
- Require Scrap P12, Service P12, Viper-9 and AR-4 to form one connected rendered component in both contexts.
- Require zero positive magazine-to-base gap and one shared reload movement path.
- Keep operator grip, support, muzzle and stock anchors derived from the same scale profile as the visible weapon.
- Preserve the single shared part model across Armoury, first-person, living operators and fallen operators.
- Run all held/viewmodel weapon checks plus retained operator, corpse, armour, command, arena, navigation and 30-second arena simulations.
- Validate syntax, live WebGL, deterministic rebuild parity and archive integrity before release.

STRIKEWATCH BUILD 12.55 OPERATOR MODEL REMASTER CONTRACT

Read every Markdown file before editing. Preserve the custom asset-free WebGL renderer and `js/60-renderer-core.js` as the procedural mesh authority. Keep the anatomical profiled head, tapered jaw, cheek/brow volume, restrained nose profile, open-bottom helmet shell, curved face cover, split framed lenses, smooth headset cups, rails, chin straps and team helmet tabs. `drawOperatorHeadAssembly()` in `js/62-character-renderer.js` must remain the single living/corpse head assembly. Keep six deterministic appearance variants, constrained by Build 12.57 to a fair/light natural skin-tone family, and restrained identity-based proportions presentation-only. Preserve the profiled pelvis, carrier, shoulder shell and glove meshes, distance-based detail tiers, `OPERATOR_PROPORTIONS`, animation rig, collision, hit detection and weapon attachment anchors. Do not change combat, AI, armour balance, rewards, economy, save schema 19 or diagnostics schema 1.

BUILD 12.55 OPERATOR-MODEL INVARIANTS
- Preserve `makeSmoothIndexedMesh()`, `makeProfiledCharacterMesh()`, `makeOperatorHeadMesh()`, `makeOperatorHelmetMesh()`, `makeOperatorFaceCoverMesh()`, `makeOperatorPelvisMesh()`, `makeOperatorCarrierMesh()`, `makeOperatorShoulderPadMesh()` and `makeOperatorGloveMesh()`.
- Keep `drawOperatorHeadAssembly()` shared by live and corpse render paths.
- Keep the anatomical head and helmet inside the established `modelTop` contract without changing headshot or collision logic.
- Keep accessories attached to the head transform and retain low, medium and full detail variants.
- Preserve deterministic appearance variation without gameplay effects or persistent save changes.
- Preserve leg IK, weapon grip/support/muzzle/stock attachment tolerances and armour-class presentation.
- Run operator head/model/presentation/corpse/weapon checks plus retained armour, command, arena and navigation regressions.
- Validate live WebGL, deterministic rebuild parity and archive integrity before release.

STRIKEWATCH BUILD 12.54 FULL-DEPTH 3D ARMOUR CONTRACT

Read every Markdown file before editing. Preserve `js/35-career.js` as the one shared procedural armour-model authority for Supply Depot and Armoury. Keep complete connected front, side and rear construction: faceted shell cores/wings, shoulder bridges, wraparound cummerbund/side structures, class-specific plates, rear plate pocket, rear MOLLE and drag handle. Supply Depot must use a large full-width perspective stage, restrained headless product form and complete 360-degree orbit; Armoury must retain the larger operator-fitted interactive viewer with drag/swipe, zoom, reset and auto-rotation. Never apply `filter`, group opacity below 1 or other flattening effects to the nested store-form, operator-body or armour-system parent rigs: require `filter: none`, opacity `1` and `transform-style: preserve-3d`, and apply lighting/dimming to individual faces. Preserve `operatorArmourRenderProfile()` as the live WebGL silhouette authority. Do not change protection, penetration, movement, handling, fatigue, price, ownership, assignment, durability, break behaviour, save schema 19 or diagnostics schema 1.

BUILD 12.54 FULL-DEPTH-ARMOUR INVARIANTS
- Preserve `careerArmourModelProfile()`, `careerArmour3dParts()`, `careerArmourStoreFormParts()`, `careerArmourOperatorBodyParts()`, `careerArmourRigMarkup()` and `careerArmourVisualMarkup()`.
- Store and detail contexts must use the shared part-based renderer; do not replace armour with a flat image or duplicate model authority.
- Keep front/back cores and wings, shoulder bridges, cummerbund, side/plate wings, rear plate, rear MOLLE and drag handle in the generated rig.
- Keep `.career-armour-store-form`, `.career-armour-operator-body` and `.career-armour-weapon-system` unflattened with `filter: none`, opacity `1` and `transform-style: preserve-3d`.
- Keep full 360-degree store orbit, large responsive stages, hover/focus pause and reduced-motion static three-quarter presentation.
- Keep Light, Medium Flex, Medium Plate and Heavy silhouettes clearly distinct in both the CSS-3D presentation and live WebGL operators.
- Preserve `armour3dPresentationForTest()` mounted checks, armour gameplay regressions, portrait/mobile-landscape containment, deterministic build parity and archive integrity.

STRIKEWATCH BUILD 12.53 LIVE COMMAND PULSES CONTRACT

Read every Markdown file before editing. Preserve autonomous operators: the manager may issue at most one broad Live Command Pulse per live round and may not select individual units, click map coordinates or directly steer operators. Present exactly three context-sensitive choices drawn from Regroup, Commit, Disengage, Hold Territory and Switch Route. Compliance must remain role/awareness/pressure-led and may be immediate, delayed or unable. Snapshot and restore every temporary tactical-profile field; never modify health, accuracy, weapon damage, armour, rewards, economy or opponent attributes. Switch Route must reuse authored engagement plans. Keep command state transient, but retain completed outcomes in the existing match report. In landscape, keep commentary, current match moments and the open chooser entirely inside the dedicated dock below the action canvas; keep the portrait chooser viewport-level and contained. Preserve `LIVE_COMMAND_PULSE_DEFS`, `liveCommandPulseAvailability()`, `issueLiveCommandPulse()`, `completeLiveCommandPulse()`, `resetLiveCommandPulseForRound()`, `liveCommandPulseForTest()`, `landscapeCommentaryForTest()`, the Build 12.52 arena/collision/navigation baselines, save schema 19 and diagnostics schema 1.

BUILD 12.53 LIVE-COMMAND INVARIANTS
- Load `js/41-live-command-pulses.js` after `js/40-match-flow.js` and before `js/50-ui-menus.js`.
- Opening or closing the chooser never spends the pulse; a successful issue spends it for that round only.
- Always offer exactly three contextual choices in ordinary play.
- Keep immediate, delayed and unable operator response paths and explain them in live presentation.
- Restore temporary approach, priority, spacing, support, hold and route fields on expiry, round end or match reset.
- Keep protected health, accuracy, damage, weapon, armour and reward configuration unchanged.
- Reuse authored engagement routes and existing autonomous navigation; do not create a second tactical or map authority.
- In landscape, require canvas-inside-stage plus feed/moment/panel-inside-dock geometry and zero horizontal overflow.
- Preserve all five command-path tests, report persistence, portrait containment, independent 30-simulated-second arena runs, deterministic output and archive integrity.

STRIKEWATCH BUILD 12.52 ARENA GEOMETRY INTEGRITY CONTRACT

Read every Markdown file before editing. Preserve the established Citadel Depot, Skyline Offices and Dune Bastion map, collision, navigation, line-of-sight, spawn and engagement-plan authorities. Solid-looking presentation geometry must be visibly grounded or attached to an authored wall, ceiling or frame. Keep Citadel stairs solid and grounded, walkways suspended, doors wall-tied and industrial fixtures supported. Keep Office baffles ceiling-mounted, glass and screens framed to architecture, all seating grounded, and the conference table plus six chairs inside open geometry with its collider aligned. Keep Dune canopy visible posts derived from `DUNE_CANOPY_PRESENTATION`, complete headers/braces/rafters, connected stall frames and closed amphora handles. Preserve `ARENA_GEOMETRY_PRESENTATION`, `arenaGeometryPresentationSnapshot()`, `arenaGeometryIntegrityForTest()`, `allArenaGeometryIntegrityForTest()`, Dune's 58-collider and 494-node/2,752-edge one-component baselines, zero support overlaps, save schema 19 and diagnostics schema 1.

STRIKEWATCH BUILD 12.51 LIVING MARKET & ORGANIC MAIL CONTRACT

Preserve the current league, player-generation, recruitment, scouting and transfer-negotiation authorities. Dynamic entrants may be generated from the club's current division or a weaker source tier only; never expose a stronger locked pool. Keep names unique across the user squad, live market and rival rosters. Market origins, availability, demand and price movement must remain visible and evidence-led. Rival bids must use role need, squad strength and persistent budget constraints, and AI signings must survive league normalisation. Commissioned searches may rotate only unprotected listings and must preserve shortlists, rival bids and active negotiations. Long-form organic email must use the existing popup/inbox presentation, remain scrollable and derive its claims from real save data. Preserve `dynamicMarketProcessDay()`, `dynamicMarketGeneratePlayer()`, `dynamicMarketSelectRivalBid()`, `clubAddOrganicMail()`, `dynamicTransferMarketForTest()`, save schema 19 and diagnostics schema 1.

STRIKEWATCH BUILD 12.50 MATCHDAY STORY & PAYOFF CONTRACT

Preserve the existing match simulation, telemetry, tactical-analysis and squad-dynamics authorities. Live story moments must be derived only from visible eliminations and existing telemetry, remain restrained and bounded, and never reveal unseen opponent intent. Round reads and post-match highlights are explanations, not result manipulation. Operator match identities require repeated evidence across at least three matches, are capped at two per operator and are presentation-only: they must never change attributes, weapons, AI, tactics, rewards, economy or progression. Keep match moment history bounded to 48, preserve `matchdayStoryPayoffForTest()`, save schema 19 and diagnostics schema 1.

STRIKEWATCH BUILD 12.49 ECONOMY GUIDANCE CONTRACT

Preserve `clubEconomyGuideMarkup()` as the single reusable explanation of Club Cash, wage headroom, the foundation loan, Gold Coins, Team XP and Player XP. Keep essential timing just-in-time: fee/wage/loan distinctions before the first signing and reward destinations after the first match. Do not reintroduce duplicate progress trackers or multiple competing currency tutorials. The Finance, Gold, Training and Supply Depot context guides should explain page use rather than restating the entire economy. Do not change prices, reward formulas, wage processing, loan collection, progression, save schema 19 or diagnostics schema 1.

STRIKEWATCH BUILD 12.48 RECRUITMENT DECISION TOOL CONTRACT

Preserve candidate Comparison as a transient, scouting-aware decision aid rather than a second transfer or player-rating authority. Two or three selected market candidates must receive Best Current Fit context, Immediate Impact, Future Ceiling, Squad Need and Budget Fit, plain-language Why This Fits and Main Trade-off explanations, retained medical/fee/wage/cash-after facts and direct Report/Negotiate actions. Different roles must be evaluated by what they add to the current Active Five, not overall alone. Keep the three-candidate cap, the second-selection focus behaviour, Build 12.47 active-negotiation route exception, transfer calculations, squad limits, save schema 19 and diagnostics schema 1.

STRIKEWATCH BUILD 12.47 RECRUITMENT NEGOTIATION FIX CONTRACT

During the First Match Guide, candidate Negotiate controls must open the existing Transfer Centre rather than creating a hidden incoming deal behind the progressive route lock. Preserve the narrow `progressiveRouteAccess()` exception: Transfers opens only when `careerState.transfers.activeIncoming` exists and the guide is in Recruitment, Profile, First Signing or Active Five. Preserve Negotiate → Submit Offer → Complete Signing, transfer finances, squad limits, save schema 19 and diagnostics schema 1.

STRIKEWATCH BUILD 12.45 CENTRED ARMOUR PREVIEWS CONTRACT

Preserve the shared part-based armour renderer and the Build 12.44 operator-mannequin inspector. Supply Depot cards and Armoury detail previews must keep the nested `career-weapon-rig` centred inside `career-armour-model-system` so the full armour silhouette remains visible in every preview. Heavy armour must remain visibly broader and deeper than lighter classes, and the live WebGL armour silhouette must still mirror the authored class differences. Do not alter armour protection, penetration, movement, handling, fatigue, price, inventory, durability or break rules. Keep `armour3dPresentationForTest()`, save schema 19 and diagnostics schema 1.

STRIKEWATCH BUILD 12.44 OPERATOR-FITTED 3D ARMOUR CONTRACT

Preserve the shared part-based armour renderer. The Armoury detail preview includes a procedural operator mannequin so armour fit and silhouette remain readable, while Supply Depot cards remain armour-only. Heavy armour must remain visibly broader and deeper than light armour and retain side, shoulder, collar, abdomen and lower protection in both the UI model and live WebGL operators. Do not alter armour protection, penetration, movement, handling, fatigue, price, inventory, durability or break rules. Keep save schema 19 and diagnostics schema 1.

STRIKEWATCH BUILD 12.39 SQUAD DYNAMICS CONTRACT

Read every Markdown file before editing. Preserve the passive, positive-only relationship model. Pair growth must settle once per completed match from shared performance evidence, and each starter may use only their strongest active partnership. Developing links provide no buff rather than a penalty; relationships do not decay and must never create mandatory conversations, decision blockers or End Day locks. Automatic mentorship grants only +6% training and match-development XP. Dynamics may adjust existing owned-bot coordination and role-execution fields but must not alter health, damage, weapon data, rewards, opponents or tactical authorities. Keep save schema 19 and diagnostics schema 1.

BUILD 12.39 SQUAD-DYNAMICS INVARIANTS
- Load `js/34-squad-dynamics.js` before career state creation and normalisation.
- Settle pair evidence once per completed owned-team match and preserve `lastSettledMatch` deduplication.
- Keep all effects neutral-or-positive; unfamiliar or weak links receive no penalty.
- Apply only the strongest active partnership per starter; do not stack all pair effects.
- Preserve tier thresholds 15/35/60/82 and persistent non-decaying scores.
- Derive mentorship automatically and restrict its effect to +6% training and match-development XP for the learner.
- Keep Rotation Watch, milestone mail and debrief cards advisory and non-blocking.
- Cap one match's debrief relationship milestones at three.
- Preserve `squadDynamicsForTest()`, persistence and duplicate-settlement checks, responsive containment, Build 12.38 tactical authority, save schema 19 and diagnostics schema 1.

STRIKEWATCH BUILD 12.38 TACTICAL ADAPTATION CONTRACT

Read every Markdown file before editing. Preserve the existing active match plan, arena engagement plans and opposition identity model as the only tactical authorities. Between rounds, permit no more than two changes across approach, engagement, priority and route. Opponent adaptation must be match-scoped, derived from visible completed-round evidence, applied through the established red-bot identity path and described to the player as an uncertain analyst forecast. Never add hidden combat bonuses or expose guaranteed unseen intent. Reparent the intervention to the document body so the transformed portrait match viewport cannot clip it. Evaluate each manager intervention against the following round's real telemetry, including a match-ending round. Keep save schema 19 and diagnostics schema 1.

BUILD 12.38 TACTICAL-ADAPTATION INVARIANTS
- `resetOpponentMatchAdaptation()` runs for every new match.
- `prepareOpponentMatchAdaptation()` may change only existing formation/approach/engagement/priority behaviours for the next round.
- `betweenRoundChangeTypes()` includes route but remains capped at two changed categories.
- Route changes reuse `engagementPlanOptions()` and `selectRoundEngagementPlan()`.
- User-facing opponent copy remains forecast/counterpoint language and does not print the exact internal target plan.
- `evaluateBetweenRoundAdjustmentForRound()` records one evidence result before the next intervention or final settlement.
- Preserve full-device modal reparenting, sticky actions, mobile containment, `betweenRoundTacticsForTest()`, `opponentAdaptationForTest()`, Build 12.37 preparation redaction, save schema 19 and diagnostics schema 1.

STRIKEWATCH BUILD 12.37 OPPONENT PREPARATION CONTRACT

Read every Markdown file before editing. Preserve the one authoritative opposition identity/scouting model, the scheduled next fixture as the preparation target and the existing tactics draft workflow. Opponent response cards only stage formation, approach, engagement and priority; no automatic counter, hidden damage/health bonus or unseen-information leak is permitted. Below 50% report depth, response recommendations and drill ordering must remain generic and the captured plan, live notices and debrief must retain unconfirmed shape/range/route wording. Fixture drills are captured coaching intentions evaluated in the debrief, not a new stat modifier. Keep save schema 19 and diagnostics schema 1.

CURRENT BUILD 12.36 PRIORITY: preserve the guided calendar lock. When `openingWeekTutorialDayRestriction()` is active, the single persistent End Day cell must be a neutral grey space with one centred padlock and no visible unavailable/follow-guide copy. Keep the complete reason in `aria-label` and `title`; unlock timing and all calendar logic remain unchanged.

STRIKEWATCH CURRENT HANDOFF — BUILD 12.36: GUIDED CALENDAR LOCK

BUILD 12.36 GUIDED-CALENDAR-LOCK INVARIANTS
- `openingWeekTutorialDayRestriction()` remains the only unlock authority.
- `.guided-lock` shows one centred padlock and hides the ordinary visible label, meta line and blocker badge.
- Accessible labels and tooltips retain the complete restriction reason and route context.
- Normal End Day, matchday and non-tutorial blocker states remain unchanged.
- Preserve one End Day DOM control, `calendarHeaderForTest()`, save schema 19, diagnostics schema 1 and all Build 12.35 tests.

CURRENT BUILD 12.35 PRIORITY: preserve the Opening Week Command flow. Keep the grouped Club Daily Agenda, six Fixture Preparation checks, event-bounded Advance to Next Event flow, routed transient summaries and tutorial-aware End Day restriction. Use the existing day settlement and blocker systems; never auto-resolve management decisions or persist transient summary state.

STRIKEWATCH CURRENT HANDOFF — BUILD 12.35: OPENING WEEK COMMAND

BUILD 12.35 OPENING-WEEK-COMMAND INVARIANTS
- `js/55-opening-week.js` loads after `js/50-ui-menus.js` and wraps established calendar/menu functions.
- End Day is unavailable during guide steps where consuming time is unnecessary. It becomes available when the first-match step needs advancement to the fixture.
- Advance to Next Event runs ordinary End Day settlement for at most 21 days and stops at the first blocker, matchday, medical, scouting, transfer, opposition-report or training milestone event.
- Do not answer a decision, choose a tactic/programme/loadout, confirm a plan or skip a fixture automatically.
- `openingWeekAdvanceSummaryState` is transient and must not enter save schema 19 or diagnostics schema 1.
- After the First Match Guide, `renderFoundationPath()` shows the Club Daily Agenda, not the retired Opening Week Handoff checklist.
- Preserve `openingWeekFlowForTest()`, `seedFirstMatchCalendarForTest()`, `seedOpeningWeekForTest()`, responsive containment and all retained release tests.

CURRENT BUILD 12.34 PRIORITY: preserve recruitment decision support. Keep the Active Five Needs panel, function-based coverage, candidate impact explanations, optional beginner recommendations, three-candidate comparison and post-signing update while leaving role weights, generation, prices, wages, AI and balance unchanged.

STRIKEWATCH CURRENT HANDOFF — BUILD 12.34: RECRUITMENT DECISION SUPPORT

BUILD 12.34 RECRUITMENT-DECISION-SUPPORT INVARIANTS
- Active Five Needs translates the contracted squad into six functions without enforcing one role of each type.
- Every market candidate explains what they add, role fit and relevant affordability or medical cautions.
- Beginner recommendation badges remain explanatory and optional; never sign, shortlist or filter automatically.
- Compare at most three market or shortlisted candidates and show role purpose, relevant attributes, ability, potential, medical risk, fee, wage, cash after signing and Active Five change.
- Comparison selection and latest signing update remain transient UI state and never enter save schema 19.
- Preserve recruitmentDecisionSupportForTest(), recruitmentRoleGuideForTest(), firstMatchGuidanceForTest(), Build 12.32 payoff and diagnostics schema 1.

CURRENT BUILD 12.32 PRIORITY: preserve the first real match as the onboarding payoff. Keep the complete deployment brief, full-device team-versus-team reveal, paused start, read-only match moments and five-stage first-match outcome with a direct routed next action.

STRIKEWATCH CURRENT HANDOFF — BUILD 12.32: FIRST MATCH PAYOFF

BUILD 12.32 FIRST-MATCH-PAYOFF INVARIANTS
- Deployment shows five active-operator portraits, four matchup facts, selected map/format and established tactical warnings before DEPLOY ACTIVE FIVE OPERATORS.
- Reparent the intro to the body so it fills the device rather than the small portrait match window. Do not progress the simulation while careerMatchIntroActive() is true.
- Match moments are read-only presentation based on legitimate live state; never expose unseen enemy information or alter autonomous decisions.
- Settle the first match once, then reveal Result, Rewards, Operators, Supporters and Next Step. The final stage keeps a direct coaching route and the detailed report remains available; mark the latest report reviewed before applying that route.
- Preserve sticky reachable actions, firstMatchPayoffForTest(), Build 12.31 demo coaching, schema 19 saves and schema 1 diagnostics.

CURRENT BUILD 12.31 PRIORITY: keep every guided-demo coach card over the portrait match viewport and hold the simulation until the fourth Watch the Round action. All four steps stay paused while visible; the final action hides the coach and releases the real one-round demo.

STRIKEWATCH CURRENT HANDOFF — BUILD 12.31: DEMO VIEWPORT COACH

BUILD 12.31 DEMO-VIEWPORT-COACH INVARIANTS
- Use one top-centred portrait placement for stages 1–4 and the completion card; never let stages 1 or 2 fall below the visible match viewport.
- Keep every `NEW_PLAYER_DEMO_STEPS[*].paused` value true. Only the final Watch the Round action may clear the pause and hide the coach.
- Preserve bounded internal scrolling on short portrait screens, the real one-round AI simulation, non-career settlement, Recruitment return and `newPlayerOrientationForTest()`.
- Preserve Build 12.30 guidance consolidation, Build 12.29 candidate scrolling, save schema 19 and diagnostics schema 1.

CURRENT BUILD 12.30 PRIORITY: preserve one authoritative opening guidance system. While `firstMatchGuidance()` is active, keep the Foundation Plan hidden, Manager Induction collapsed as Optional Context with no route button, one-time section tutorials and Command Index recommendations delayed, and the Command Centre duplicate next-action aside absent.

STRIKEWATCH CURRENT HANDOFF — BUILD 12.30: GUIDANCE CONSOLIDATION

BUILD 12.30 GUIDANCE-CONSOLIDATION INVARIANTS
- The nine-step First Match Guide owns visible progress and the next route/action.
- Legacy six-stage tutorial flags remain compatible but are supporting explanation only.
- Optional Context must stay closed by default, readable when expanded and non-navigational.
- Foundation Plan remains hidden during the guide; Build 12.35 uses its post-guide slot for the Club Daily Agenda.
- Preserve Build 12.29 exact candidate scrolling, Build 12.28 orientation and Build 12.27 route locks.
- Required gate: `guidanceConsolidationForTest().ok === true`.

CURRENT BUILD 12.29 PRIORITY: preserve exact guided recruitment navigation. Recruit Operator and Continue Recruiting must route to Recruitment, scroll only the management content pane to the candidate list and leave document/shell/layout scroll plus keyboard focus unchanged.

STRIKEWATCH CURRENT HANDOFF — BUILD 12.29: GUIDED RECRUITMENT SCROLL

Read 00-READ-FIRST-GPT.md, PROJECT.md, AGENTS.md and README.md before editing. Work in modular source files only, run `python3 build.py`, and validate source plus generated standalone JavaScript before packaging.



BUILD 12.29 GUIDED-RECRUITMENT-SCROLL INVARIANTS
- Preserve scrollTarget metadata on the first-signing and active-five guidance steps.
- Preserve data-team-scroll-target on the guide action and data-guide-target on both base and enhanced Recruitment candidate lists.
- Restore the Recruitment subview to market, route, then call scrollMenuGuideTargetIntoView(); use the existing management scroller authority rather than page-level scrollIntoView.
- Do not force candidate focus or change tutorial completion, recruitment state, persistence, save schema 19 or diagnostics schema 1.
- Preserve firstMatchGuidanceForTest(), responsive containment and every Build 12.28 orientation invariant.

BUILD 12.28 GUIDED-DEMO ORIENTATION INVARIANTS
- Offer the concept briefing only to a newly created, zero-match, zero-operator club without a prior completion/skip marker.
- Keep tutorial persistence inside `careerState.tutorial.contextSeen`; transient stage/timer/winner state must never enter the save.
- Run a real Citadel AI round with `matchTarget = 1` only while the demo is active. Do not script a winner or alter combat stats.
- Exit before career settlement. Demo play must not change totals, finance, rewards, condition, league or reports.
- Preserve four coach stages, hold all four while their card is visible, release the live round only after Watch the Round, retain manual spectator controls, demo objective/plan/intention copy and the route back to Recruitment.
- Preserve `newPlayerOrientationForTest()`, modular/standalone parity, responsive overlay containment, save schema 19 and diagnostics schema 1.


BUILD 12.25 READABLE-INTERFACE AND FIXED-VIEW INVARIANTS
- Keep `maximum-scale=1,user-scalable=no` in the mobile viewport. Do not restore pinch zoom or add custom gesture-blocking JavaScript without an explicit later product decision.
- Preserve authored management typography floors across primary navigation, section tabs, date/status copy, priority panels, tutorials, section hubs, command-centre details, line-up status, progress summaries and workflow controls.
- Preserve the 320–350px fit adjustments and landscape-phone rules through 900px. Do not shrink meaningful management copy back to 5–7px to solve layout issues.
- Keep the visible compact club date and full accessible date labels. Do not globally scale the app or alter the match HUD/WebGL/gameplay systems.
- Preserve typographyConsistencyForTest(), the fixed-viewport onboardingClarityForTest() gate, random-name behaviour, schema 19 saves, schema 1 diagnostics and every retained Build 12.24/12.23 regression.

BUILD 12.24 ONBOARDING-CLARITY AND ACCESSIBILITY INVARIANTS
- Team creation must explain that the player manages the club while AI operators move, aim and shoot autonomously. Keep the first-to-three match format and promotion to the Pro League visible.
- Preserve the Recruit → Prepare → Watch → Improve loop on creation and the existing six-stage manager induction after creation.
- Keep the editable RANDOM NAME action. Names must be fictional, normalised by the existing 24-character career-name rule and must not persist or create the club until normal creation is submitted.
- Keep onboarding graphics inline/asset-free, first-run controls at least 44px high and visible keyboard focus. Build 12.26 supersedes the original pinch-zoom rule.
- Preserve randomTeamNameForTest(), onboardingClarityForTest(), schema 19 saves, schema 1 diagnostics and every Build 12.23 purchase/state regression.

BUILD 12.23 SUPPLY PURCHASE-CONFIRMATION INVARIANTS
- Successful direct weapon/armour purchases immediately refresh owned copies, remaining cash, inline receipt and button state.
- Lock the replacement button for 0.9 seconds as `PURCHASED · OWNED N`; queued/rapid taps during that lock must not transact.
- After the lock, intentional repeat purchases remain available through `BUY ANOTHER`. Preserve store scroll position.
- Purchase feedback is transient UI state only; do not persist it in careerState or change save schema 19.
- Preserve cashWeaponPurchaseFeedbackForTest(), cashWeaponStoreForTest(), exact deductions, finance ledger entries and all retained 12.22 gates.

BUILD 12.22 SUPPLY DEPOT WEAPON-STOCK INVARIANTS
- Keep direct cash offers for AR-4 Sentinel (58,000 CR) and Viper-9 Compact (32,000 CR), with AR-4 first for mobile discoverability.
- Every store weapon card must render the same careerWeapon3dMarkup/careerWeaponVisualParts geometry used by Team Armoury. Do not create store-only gun art.
- Store CSS may frame/scale the shared model only. Keep cards and previews inside 320–430px portrait and 844x390 landscape widths.
- Successful direct purchases add one finite copy, deduct the exact price and log a WEAPON finance transaction.
- Preserve cashWeaponStoreForTest(), direct AR/Viper purchase tests and all retained combat, armour, operator, map, state and build gates.


BUILD 12.21 RELOAD-COVER AND HIT-REACTION INVARIANTS
- A reloading operator with no loaded alternate weapon must use the existing cover/fallback system when exposed; do not cancel or restart the reload.
- A distinct loaded alternate prevents the forced no-alternate cover rule. Finishing the reload before arrival must not abandon an already selected cover anchor.
- Cosmetic hit reactions remain separate from gameplay flinch. Flinch is non-fatal, probabilistic, capped at 44%, lasts 0.11–0.22s and has a 0.58–0.82s anti-chain cooldown.
- Flinch may interrupt bursts and briefly displace aim/movement, but must not cancel reloads, weapon swaps, armour resolution, navigation or saves.
- Preserve reloadCoverBehaviourForTest(), hitReactionFlinchForTest(), diagnostic counters/events, schema 19 saves and schema 1 diagnostics.

BUILD 12.20 OPERATOR-HELD AR-4 INVARIANTS
- Keep AR-4 geometry and functional hand anchors authoritative in js/35-career.js.
- OPERATOR_LONG_GUN_POSE may move the complete third-person rifle rig only; preserve the dominant-side offset, ready-height lift and shoulder pullback.
- The butt pad must remain seated in the dominant shoulder pocket. operatorWeaponAttachmentForTest() must keep AR-4 stockSeatGap at or below 0.075m.
- Dominant/support hands remain attached to pistol-grip/support-grip. Do not replace these with generic hand offsets.
- Preserve operatorHeldPoseForTest() as a deterministic live WebGL visual-regression helper.
- Do not change first-person placement, model geometry, reload behaviour, weapon balance, collision, hit detection, AI, save schema 19 or diagnostics schema 1.

BUILD 12.19 AR-4 MODEL INVARIANTS
- The AR-4 model is authored once in js/35-career.js. Do not create renderer-only rifle dimensions or parts.
- Shared parts may use box, rounded or cylinder-length shapes. Armoury CSS, first-person WebGL and operator WebGL must consume the same shape metadata.
- The tube scope and every `optic-*` part are removed. Preserve the lower top rail and front/rear flip-up iron sights; do not recreate a renderer-only scope.
- Preserve the refined stock, receiver and handguard proportions and keep `scopeRemoved` / `lowProfileSights` true in the AR-4 audit.
- Preserve pistol-grip/support-grip anchors, model-bounds muzzle placement, magazine/reload animation and Build 12.17 primary/sidearm behaviour.
- Preserve careerAr4ModelAudit() and window.__strikeDebug.ar4WeaponModelForTest(), plus attachment/loadout mapping tests.

BUILD 12.17 PRIMARY/SIDEARM INVARIANTS
- Save schema 19 persists primary and sidearm separately and migrates schema-18 and older careers. Legacy pistols become sidearms; legacy AR-4 loadouts keep the rifle and receive a Scrapline sidearm.
- Count finite weapon copies across both slots. Use the canonical player slot helpers; `equippedWeaponId` is compatibility state, not the slot authority.
- Sidearms are always available; a sidearm-only operator has no dedicated primary.
- Preserve independent magazines/reserves and timed AI switching. Close emergency or unsafe empty-primary states may draw the sidearm; range/safety may return to primary. Record and explain switches.
- Preserve AR-4 movement/turn/moving-accuracy/sprint-settle/fatigue/reload/noise costs and pistol draw/handling/mobility benefits. Price alone is not balance.
- Every usable weapon card communicates range, slot, damage, magazine, penetration, movement cost, benefits and limitations.
- Spectator HP area displays armour integrity; portrait shows HP and ARM. Match/debrief data retain sidearm draws.
- Release gates include weaponSlotSystemForTest, weaponRoleBalanceForTest, weaponSwitchingForTest, spectatorArmourHudForTest, mixed loadout mapping, schema-19 state integrity, live combat and all retained map/AI/runtime checks.

OPERATOR MODEL INVARIANTS
- Keep the custom asset-free WebGL renderer. Preserve Build 12.08 geometry, Build 12.09 commitment/motion/quality systems, the Build 12.10 staggered-acquisition correction, the Build 12.11 corpse/render-loop stability fix, the Build 12.12 AI pacing/team-spacing layer and the Build 12.13 tactical readability/coaching layer and the Build 12.14 model-driven weapon attachment layer.
- Shared meshes are created once: shaped torso, tapered capsule limbs and rounded major equipment forms. Never generate these per operator or per frame.
- Living and corpse renderers use the same upgraded body geometry.
- Preserve two-bone legs, animation blending, hit/death reactions, weapon anchors, operator height and broad-shouldered gameplay readability.
- Do not change collision, hit detection, line of sight, navigation, combat values, economy, progression or save schema to match the visual mesh.
- Keep the body pass draw-call neutral. Preserve `operatorSurfaceGeometryAudit()`, `operatorModel()` and `operatorPresentationForTest()`.

CURRENT ARENAS
- Citadel Depot: original industrial map.
- Skyline Offices: office/courtyard map with automatic doors and existing Office clearance audits.
- Dune Bastion: current Map 3. Summit Terminal has been removed and is historical.

DUNE BASTION INVARIANTS
- Arena ID `dune`, name DUNE BASTION, theme `desert`.
- 36 × 24, exactly one floor, no stairs, no doors, exact left/right wall symmetry and one connected navigation graph component.
- Mirrored gameplay collider positions/dimensions. Decorative colours may vary.
- Original arid-fortress atmosphere only: sandstone/plaster, packed-earth floor, open sky, arches, canopies, banners, stalls, palms, pots, crates, sandbags and a well. Do not copy another game's map layout/assets.
- Retain the 12.05 visual layer and 12.06 alignment corrections: perimeter crenellations, exterior dunes/rocks/corner towers, mosaics/pavers/rugs, wall-backed braziers with plates, striped canopies and high-detail asset-free props.
- Ordinary solid props plus visible canopy, arch and banner ground supports use authored collision. Exterior backdrop, wall-top details, rubble, mosaics, cloth, banner fabric/tails, plaques and lintel facing remain non-blocking.
- Every standard banner has a grounded stone foot, continuous mast, visibly connected rods and a matching `banner-post` collider. Never animate the entire support assembly.
- Every arch inner lintel overlaps/touches its parent masonry. Landmark emblems remain flush-backed plaques on arch faces, never isolated roof-line blocks.
- Every brazier requires a real wall behind its anchor, open space in front and a visible mounting plate.
- Support integrity audits must remain clean: zero `supportOverlaps`, zero `supportPairOverlaps`, all six banners grounded, all six arch attachment snapshots valid and `allDecorSupportsClear === true`.
- Keep procedural detail bounded and mobile-friendly; no imported assets, per-frame random geometry or dense particles.
- North Rampart supports long-range play; Central Gate supports mixed engagements; South Bazaar/courtyard routes support close-range play and flanks.
- Six engagement plans and fourteen hotspots must be directly clear and reachable from all spawns.
- Live minimap and generated deployment preview derive from the same arena definition.
- Free Roam, matchmaking, diagnostics and new saves use `dune`. Keep `arenaMeta('summit') -> dune` only for old-save migration.

DUNE RELEASE GATES
- `duneBastionAuditForTest().ok === true`
- 58 live colliders, one 494-node/2,752-edge navigation component
- eight wall-mounted braziers, six grounded standard banners, six attached arch-detail checks
- zero ordinary/support and support/support overlaps
- `arenaAuditForTest('dune')`: connected and all spawns clear
- `engagementPlanAuditForTest('dune').ok === true`
- `navigationBenchmarkForTest('dune', 160)`: 160 successes, zero failures, one graph component
- `duneDeploymentPreviewForTest()`: rendered with non-empty image data
- Free Roam lists/enters Dune on ground floor and exports the correct ID/name
- Retain all Citadel, Office, management, tutorial, state-integrity, diagnostic, responsive UI, syntax, deterministic-build and ZIP checks

RETAINED GLOBAL CONTRACTS
- Career save schema 19 with schema-18-and-older migration; diagnostics schema 1 with optional armour fields.
- Configuration Free Roam remains operator-collision inspection, not noclip.
- Cached navigation graph, bounded failed-goal recovery, diagnostic circular buffers and scheduling-gap/update-stall separation remain active.
- Inbox remains two visible rows before scrolling.
- Player profile remains the individual telemetry authority; loadout issue autosaves; team telemetry remains aggregate.
- Supporter expectations, scouting, opponent identities, team ratings, multi-kill rewards and management flows remain intact.
- No hidden map-specific health, damage, accuracy, reward or AI-strength bonuses.

Historical Summit functions/notes may remain for release history or schema compatibility, but they are not current Map 3 requirements and should not be run as Dune release gates.

RETAINED BUILD 12.16 ARMOUR ATTRITION INVARIANTS
- Armour is finite club inventory: one purchased `armourInventory` entry protects one `equippedArmourId` player. `none` is unlimited.
- Torso-only graded protection compares weapon penetration with armour rating after resilience. Headshots bypass. Never implement complete immunity.
- Integrity persists through all rounds of the current match. Only a newly created match services surviving armour to full. Zero integrity immediately marks the rig broken, removes an owned copy, unequips the player and logs the loss; broken opposition armour also stays absent for the rest of the match.
- Heavy protection must carry larger movement, handling and fatigue penalties. Do not turn price/rarity into unconditional power.
- Armour silhouettes are presentation only and must not change hitboxes or collision.
- Preserve armourSystemForTest, armourMatchAttritionForTest, seedArmourLoadoutsForTest, armourLoadoutMappingForTest and equipPlayerArmourForTest plus schema-19 round trips, store/loadout mobile checks and live combat diagnostics.

RETAINED BUILD 12.14 INVARIANTS
- Shared P12/Viper/AR-4 operator hands resolve from authored grip/support parts; AR-4 dominant grip is `pistol-grip`.
- Third-person model origin, hand anchors and shared-model muzzle flash use the same scale, pitch and roll transform.
- Apply body bob once to hands and weapon. Never reintroduce the prior vertical drift.
- Far AR-4 LOD retains `pistol-grip` and `support-grip`.
- Weapon/loadout rendering changes must not alter weapon stats, inventory, hit logic, AI or schemas.
- Preserve and run operatorWeaponAttachmentForTest and operatorWeaponLoadoutMappingForTest.

RETAINED BUILD 12.13 INVARIANTS
- Deployment route lines, lane cards and likely contact derive from the live engagement-plan data; round one consumes the captured openingPlanId and matching objectives.
- Live explanations describe only the currently viewed owned operator and existing visible/confirmed state. Never reveal hidden enemies or internal scoring.
- Persist five owned operator diagnostic summaries into the completed report and build intent-versus-execution plus role reviews from real match telemetry.
- Coaching buttons navigate/highlight Tactics, Training and player Loadout destinations without silently applying changes or bypassing reward presentation.
- Preserve tacticalPreviewForTest, openingPlanParityForTest, liveDecisionExplanationForTest, tacticalCoachingAnalysisForTest and tacticalCoachingDestinationForTest.

RETAINED BUILD 12.12 INVARIANTS
- Reset shared perception/tactical work budgets once per rendered frame before fixed simulation substeps.
- Never budget away current-target or pending-candidate LOS/reaction truth; only broad scans and ordinary tactical reconsideration may defer.
- Combat approach routing performs bounded cheap candidate scoring followed by at most one budgeted A* search.
- Preserve tactic/role/engagement-aware spacing. Stay Grouped is compact but non-overlapping; Trade, Hold and Flank widen lanes.
- Dynamic goals and traffic costs account for teammate positions, reservations, next path points and duplicate threat angles.
- Preserve and run botWorkBudgetForTest, combatRouteBudgetForTest and teamSpacingForTest plus multi-round Dune smoke testing and current real-device diagnostics.

RETAINED BUILD 12.11 INVARIANTS
- Corpse detail flags are local to drawCorpse and derived from its own runtime distance tier.
- The first elimination must render all corpse poses and distance LODs without throwing.
- The requestAnimationFrame loop reschedules in finally and records bounded runtime faults in diagnostics.
- Preserve and run corpsePresentationForTest, firstEliminationContinuityForTest and runtimeFaultsForTest.

RETAINED BUILD 12.10 INVARIANTS
- A visible unconfirmed sight candidate is revalidated every frame and is not cleared merely because a full challenger scan was skipped.
- Initial recognition time advances every simulation frame; broad-scan cadence must never prevent target acquisition or combat.
- A visible enemy occluding the current enemy may trigger immediate retargeting; friendly body occlusion remains authoritative.
- Preserve and run staggeredPerceptionAcquisitionForTest, closeThreatRetargetForTest and operatorViewOcclusionForTest plus a Dune combat smoke simulation.

RETAINED BUILD 12.09 INVARIANTS
- Current-target LOS remains per-frame; only broad challenger scans are staggered.
- Preserve target/tactical commitment hysteresis and immediate-threat/emergency overrides.
- Validate dynamic goals for walkability, clearance and team reservations before one bounded A*.
- Preserve reversible runtime quality tiers and distance-based operator/weapon LOD; never use one flagship phone as the acceptance baseline.
- Preserve render-only turn anticipation, foot planting, breathing/aim stability and body lean.
