STRIKEWATCH CONCISE HANDOFF

Read `HANDOFF.md` and `AGENTS.md` only. Use the routing table in `HANDOFF.md`
to load the relevant section of `CONTRACTS.md`, `ARCHITECTURE.md` or one
historical audit. Do not read every Markdown or `AUDIT-*.md` file.

CURRENT RELEASE
Build 12.149 — Grip Rake
Build ID: 12.149.0-grip-rake
Save schema: 19
Diagnostics schema: 1

SOURCE AND RELEASE
`strikewatch-source/` is editable.
`js/strikewatch.dev.js`, `dist/*.html` and root `cod.html` are generated or
copied release artifacts and must not be edited independently.

CURRENT HAZARD
Build 12.149 owns the grip rake convention: +x is toward the muzzle, +y is
down, and a POSITIVE gripRz rakes the grip rearward. Verify by reading the
rendered mag-base x against grip x, not by eye.
Build 12.148 owns the sidearm silhouette and the rigid grip assembly; rz
rotates about a part's own centre, and weaponGeometryIntegrityForTest must be
re-run after moving any weapon geometry.
Build 12.147 owns weapon presentation; careerWeaponVisualParts() is the single
geometry authority for menus, operator weapon and viewmodel alike.
Build 12.146 owns baked occlusion; keep it quantised or the static batcher
fragments. Do not batch another arena until animated draws in drawStaticWorld
are excluded via setStaticWorldBatchEligibility().
Build 12.145 owns the shared surface noise term and valueNoise(); six surface
modes read it, so changes there affect every arena. Keep detail below ~40
cycles per world unit or it aliases.
Build 12.144 owns the desert sandstone surface (uSurface == 7) and the Dune
wall tilework; keep surface variation structured, not hashed noise.
Build 12.143 owns the open-air sky (js/65-sky-dome.js); desert only, no depth
writes, and sky colour is not derived from fog colour. Re-check the Dune nav
baseline (494 nodes / 2752 edges / 1 component) after touching it.
Build 12.142 owns the armour viewer rotation pivot; never animate by writing
an inherited custom property onto an ancestor of a big CSS-3D subtree.
Build 12.141 owns career save checkpoints; no path back to HQ may skip a save,
and page-hide/unload must write. Weapon geometry has two authorities:
careerWeapon3dParts (menus) and operatorSharedWeaponRig/drawFirstPersonWeapon
(live match) — change both.
Build 12.140 owns the management status surface and careerMatchLaunchState();
a control that can refuse must say so before it is pressed and route to what
clears the blocker. showStatus() alone is invisible outside a match.
Build 12.139 owns the training requirement statement; the workflow draft bar
stays beside the roster it saves, the panel head states what is outstanding,
and a *ForTest() hook must suppress persistence while it mutates careerState.
Build 12.138 owns guided scroll reachability; a guide step whose control is
off the arrival screen needs a scrollTarget plus a matching data-guide-target
anchor, and menuHistoryScroller() must resolve the element that actually
scrolls on the active presentation target.
Build 12.137 owns victory-crate persistence; any earned reward must reach the
career save before it is shown, never only module state.
Build 12.136 owns the scroll-container row sizing; size the grid tracks, not
the items, and keep the compact audit at zero overlapping and zero collapsed.
Build 12.135 owns the grid container-collapse fix, league settlement
recovery and the league/agenda/training readability rules; keep the compact
audit reporting zero collapsed containers and never let a league result settle
to null.
Build 12.134 owns career save sequencing and read-back verification, the
compact readability/containment layer, the inherited founding assistant and the
post-match fixture-type descriptor; keep saves sequenced and the compact audit
at zero overflow.
Build 12.133 owns the readability/alignment/portrait layer at the end of
`css/game.css`, the Inbox scroll guard, the recommended-plan change warning and
the per-player decision cooldown; keep the feed inert until clicked and the
portraits deterministic and asset-free.
Build 12.132 owns the match setup selection feedback layer at the end of
`css/game.css` plus `js/76-tactical-selection-feedback.js`; keep the choice
cards' selected, pressed, hover, focus and confirmation states distinct and
keep that layer last in the cascade.
Build 12.131 owns the tactical summary tags, Active Operator Match Roles
alignment and the persistent End Day / Next Day contrast.
Build 12.130 owns the Aurora Terminal arena; see `AUDIT-12.130.md` before
changing it.
Build 12.129 owns the First Match Guide forward-only flow and the opening-week
progression release.
Build 12.128 owns the portrait-windowed commentary strip between the scoreboard
and objective; commentary must not overlay the arena and must return to the
existing match-view position outside portrait windowed mode.
Build 12.127 owns the compact combat-effectiveness score header, readable
legend, focused plot, responsive stat tiles and 320px single-column fallback.
Build 12.126 retains compact tactics row sizing, the single normal-flow final
check, readable guided Next Day status, and fixture-persistent preparation.
Build 12.125 retains the desktop Inbox and translucent command chrome.
Builds 12.122–12.123 own the Command Skin layers near the end of
`css/game.css`; visual changes belong there and must stay chrome-only.
Preserve the 12px meaningful-copy and 14px explanatory floors, the mobile
tactics action and content-sized sections, modal focus isolation, compact
Compare access, and the guided `NEXT` rail reset and visible badge geometry.

RELEASE
Update the smallest relevant authority and current audit, build, parse, run
targeted and adjacent checks, verify responsive containment where relevant,
rebuild deterministically, copy the standalone to `cod.html`, then commit and
push source/docs/artifacts together.

Historical detail is available through `CHANGELOG.md` and individual audits.
