name: Release Kerbside 0.6.53

on:
  workflow_dispatch:

jobs:
  disabled:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Repository-standard Kerbside validation owns this release.'
